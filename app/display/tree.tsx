'use client';

/**
 * ЗАГЛУШКА сцены дерева. Сюда приходит PixiJS.
 *
 * Компонент грузится только в браузере — см. tree-mount.tsx. Именно поэтому
 * импортировать pixi.js можно здесь и нельзя выше по дереву: на сервере он
 * падает, и сборка на Vercel развалится.
 *
 * Что здесь появится, по разделам 8 и 10 ТЗ: детерминированная от seed
 * генерация дорожек, точки крепления листьев, опрос GET /api/wishes по
 * курсору updated_at, импульс тока к новому листу, карточка пожелания,
 * режим покоя. Ничего из этого каркас не угадывает.
 */
export default function Tree() {
  return (
    <p style={{ color: 'var(--copper-dim)' }}>
      Здесь будет дерево. Сцена PixiJS, клиентский компонент, ssr отключён.
    </p>
  );
}
