.next
node_modules
out
package-lock.json
next-env.d.ts

# Документы проекта: ТЗ, задачи, правила для агентов.
# Prettier перекладывает в них переносы строк и списки. Это чужой текст,
# написанный человеком, и в diff он приезжать не должен.
docs/
AGENTS.md
CLAUDE.md

# Замороженный контракт. Против него пишется визуализация дерева, и файл
# должен оставаться ровно таким, каким его согласовали. Прогон форматтера
# показал бы правку в файле, который «менять только по согласованию».
lib/types.ts
